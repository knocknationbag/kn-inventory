# Copy to .env.local for local development. Set the same two variables in Vercel
# (Project Settings -> Environment Variables). Both values are public by design
# (protected by Row Level Security). NEVER add a service-role key or any password here.
NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxxxxxxxxxxxxx
